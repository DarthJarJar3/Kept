# Kept

A three-screen prototype for saving recipes the way you actually cook them, then finding them again.

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:43123](http://localhost:43123).
